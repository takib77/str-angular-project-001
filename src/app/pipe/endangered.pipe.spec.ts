import { EndangeredPipe } from './endangered.pipe';

describe('EndangeredPipe', () => {
  it('create an instance', () => {
    const pipe = new EndangeredPipe();
    expect(pipe).toBeTruthy();
  });
});
